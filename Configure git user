# Configure git user
git config user.name "Shristi Khare"
git config user.email "your-email@gmail.com"

# Add all files
git add .

# Create initial commit
git commit -m "Initial commit: Full-stack job application tracker"

# Add GitHub remote (replace with your GitHub username)
git remote add origin https://github.com/Shristikhare/job-application-tracker.git

# Push to main branch
git branch -M main
git push -u origin main
